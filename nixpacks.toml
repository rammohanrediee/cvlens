[providers]
python = {}

[phases.setup]
nixPkgs = ["python311", "gcc", "tesseract"]

[phases.install]
cmds = [
  "python3 -m pip install --upgrade pip",
  "python3 -m pip install -r requirements.txt",
  "python3 -m pip install ."
]

[start]
cmd = "bash ./start-backend.sh"
